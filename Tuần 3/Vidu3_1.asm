# Ví dụ 3.1 Tuần 3
# Chương trình cộng

# Toán tử thanh ghi R
li $t1, 100
li $t2, 50
add $t0,$t1,$t2

# Cộng với giá trị tức thời
addi $t0,$t0,50
add $t0,$t0,50

addiu $t0,$t2,-100

addi $t1,$t2, 5647123
